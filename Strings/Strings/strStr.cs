namespace Strings.Strings
{
    public class strStr
    {
        public int MyStrStr(string haystack, String needle)
        {
            //use built-in index to find first needle in haystack
            return haystack.IndexOf(needle);
        }
    }
}
